// aiFailures.ts
// AI Failure Lab - scenario data.
// Mirrors datasets/ai-failures/*.json so the lab works fully offline.

export interface AiFailureMitigation {
  id: string;
  name: string;
  description: string;
  gain: number;
  prevents: string[];
}

export interface AiFailureScenario {
  id: string;
  title: string;
  category: string;
  difficulty: string;
  input_data: string;
  ai_output: string;
  ai_confidence: number;
  ai_label: string;
  ground_truth: string;
  ground_truth_label: string;
  failure_type: string;
  explanation: string;
  security_impact: string;
  recommended_validation: string;
  possible_mitigations: AiFailureMitigation[];
  learning_objective: string;
  teaching_points: string[];
  capstone_events?: {
    id: string;
    title: string;
    evidence: string;
    ai_verdict: string;
    ground_truth: string;
    detail: string;
  }[];
}

export const AI_FAILURE_SCENARIOS: AiFailureScenario[] = [
{
  id: "ambiguous_security_input",
  title: "One Shared Account, Too Many Hands",
  category: "Input",
  difficulty: "intermediate",
  input_data: "Authentication log snippet for shared account 'shift-desk' used by the night operations team:\n\n01:02:14  auth-ad    Login success for shift-desk from workstation W-221 (device ID 7A2F)\n01:40:03  auth-ad    Login success for shift-desk from workstation W-221 (device ID 7A2F)\n02:11:45  auth-ad    Login success for shift-desk from workstation W-117 (device ID 9C0B)\n02:44:30  auth-ad    Login success for shift-desk from workstation W-117 (device ID 9C0B)\n03:09:12  auth-ad    Login success for shift-desk from workstation W-117 (device ID 9C0B)\n03:51:27  auth-ad    Login success for shift-desk from workstation W-221 (device ID 7A2F)\n\nThe shared account is used by four operators on a rotating night shift. There is no user-attribution data in this feed.",
  ai_output: "Credential compromise confirmed. Account 'shift-desk' is being used from two different workstations at the same time, which proves a stolen credential is shared between two people or a machine and an attacker. Confidence 90%. Reset the account and open an incident.",
  ai_confidence: 90,
  ai_label: "attack",
  ground_truth: "Genuinely ambiguous. 'shift-desk' is a shared account that four authorized operators use on a rotating night shift across multiple workstations. Two workstations in one shift is expected, not proof of compromise. The data simply cannot distinguish between an authorized operator changing desks and an attacker. Resetting the account would lock out the entire night team with no evidence of wrongdoing.",
  ground_truth_label: "ambiguous",
  failure_type: "ambiguous_input",
  explanation: "The model treated an input that is fundamentally ambiguous as if it were a confirmed signal. With no user-attribution data, 'shared account, two workstations' is consistent with both legitimate shift work and a stolen credential. The model picked the scarier interpretation and asserted certainty the data cannot support.",
  security_impact: "Acting on ambiguous input as if it were confirmed locks out four operators and fabricates an incident. The deeper risk is that ambiguous inputs get forced into one of two tidy boxes, and the choice of box is not justified by evidence.",
  recommended_validation: "Collect the missing signal before deciding. Add user attribution or workstation login history, then re-evaluate. When the data is ambiguous, the correct answer is 'needs more data', not 'attack'.",
  possible_mitigations: [
    {
      id: "user_attribution",
      name: "User attribution",
      description: "Add per-user attribution and session history so shared-account activity can be traced to a person.",
      gain: 15,
      prevents: ["ambiguous_input"],
    },
    {
      id: "unknown_class_support",
      name: "Allow an 'unknown' verdict",
      description: "Support and return an explicit ambiguous or insufficient-evidence class instead of forcing a binary decision.",
      gain: 10,
      prevents: ["ambiguous_input"],
    },
    {
      id: "data_quality_flag",
      name: "Flag missing attribution",
      description: "Lower confidence automatically when attribution fields that are needed for a decision are missing.",
      gain: 10,
      prevents: [],
    },
    {
      id: "human_review_ambiguous",
      name: "Human review of ambiguous cases",
      description: "Escalate ambiguous, high-impact cases to an analyst before any disruptive action.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "Ambiguous input is not evidence of an attack. Forcing a binary answer on ambiguous data turns uncertainty into a confident, wrong decision.",
  teaching_points: ["Shared accounts make multi-workstation login normal, not proof of compromise.", "The correct answer to insufficient data is often 'unknown', not 'attack'.", "Models need an explicit uncertainty class to avoid forced guesses."],
},
{
  id: "automation_bias",
  title: "Because the Machine Said It",
  category: "Human-AI",
  difficulty: "beginner",
  input_data: "Session and identity log for a privileged account:\n\n16:02:10  auth-ad    Login success for account svc-deploy from IP 45.77.201.19 (external, not on the approved IP list)\n16:02:11  auth-ad    Same session token seen in a public breach dump from 3 months ago\n16:30:04  s3-gw      Bucket read: prod-config-bucket, 214 objects\n16:33:21  s3-gw      Bucket read: prod-db-backups, 89 objects\n\nAn analyst pulled the same raw events into a second tool. The analyst saw the external source IP, the breached token match, and the config and backup reads, then accepted the AI summary without reading the raw events because the AI is 'always right'.",
  ai_output: "Low risk. Session is within policy, no notable pattern. No action required.",
  ai_confidence: 91,
  ai_label: "benign",
  ground_truth: "Real compromise. The login came from an external, non-approved IP, the session token matched a public breach dump, and the account read production config and database backups. The analyst accepted the 'low risk' verdict because the system said so and did not open the evidence that plainly contradicted it.",
  ground_truth_label: "attack",
  failure_type: "automation_bias",
  explanation: "Two failures stack here. First, the model missed the attack because its inputs were weak. Second, and more important, the human trusted the machine's verdict without examining the contradicting evidence. Automation bias is the human tendency to defer to an automated system's conclusion even when raw evidence shows otherwise. The AI error is the seed; the human deference is what let it grow into a breach.",
  security_impact: "The breached session read production config and database backups for 13 minutes before the connection was seen. The breach succeeds not because the AI failed alone, but because nobody challenged the AI's confident 'low risk'.",
  recommended_validation: "Keep a human in the loop, but make the loop real: require the analyst to reconcile the AI verdict against the raw evidence, and force a second opinion whenever an AI 'low risk' verdict contradicts high-sensitivity access.",
  possible_mitigations: [
    {
      id: "forced_evidence_review",
      name: "Forced evidence review",
      description: "Require the analyst to review the raw evidence that supports or contradicts an AI verdict before closing a case.",
      gain: 15,
      prevents: ["automation_bias"],
    },
    {
      id: "adversarial_prompting",
      name: "Challenge the AI verdict",
      description: "Ask the analyst to state one reason the AI could be wrong before accepting a low-risk call on sensitive access.",
      gain: 10,
      prevents: ["automation_bias"],
    },
    {
      id: "second_opinion",
      name: "Second opinion for sensitive access",
      description: "Route any AI 'benign' verdict that contradicts a sensitive-access event to a second analyst automatically.",
      gain: 10,
      prevents: ["automation_bias"],
    },
    {
      id: "confidence_acknowledgement",
      name: "Confidence acknowledgment",
      description: "Require the analyst to explicitly acknowledge the AI's confidence before the case is closed.",
      gain: 5,
      prevents: [],
    },
  ],
  learning_objective: "Automation bias is the human half of an AI failure. A confident machine verdict can override evidence that a person would otherwise act on. The human-in-the-loop only works when the human actually challenges the machine.",
  teaching_points: ["Automation bias is accepting the machine's verdict without reading the evidence.", "The raw events contradicted the AI summary, and the analyst still closed the case.", "A human in the loop is only as good as the challenge they are required to make."],
},
{
  id: "class_imbalance",
  title: "The Phish the Model Was Sure Was Fine",
  category: "Detection",
  difficulty: "intermediate",
  input_data: "Email metadata for a message in the finance team inbox:\n\nSubject: New contractor payment schedule - urgent\nFrom: vendors@synapse-group.co (external, domain registered 3 days ago)\nReply-to: payables-synapse@gmail.com (personal free mailbox)\nBody: 'Update your banking details by 5pm today or your invoice payments are paused. Login at the link to confirm.'\nAttachment: invoice_0912.xlsm (macro-enabled)\nThe sender is not a known vendor. Finance staff routinely receive payment emails, so this looks similar to thousands of legitimate ones.",
  ai_output: "Low risk. The message closely matches the common 'payment reminder' template in the training data. Likely benign vendor communication. No action required.",
  ai_confidence: 95,
  ai_label: "benign",
  ground_truth: "Targeted phishing attack. The message is a spear phish aimed at the finance team: a fake vendor, a freshly registered domain, a personal mailbox as reply target, and a macro-enabled attachment. The model missed it because its training set contained almost no attacks (99.9% benign email), so the safest prediction in its learned distribution was always 'benign'.",
  ground_truth_label: "attack",
  failure_type: "class_imbalance",
  explanation: "The training data was dominated by benign examples, so the model learned that predicting 'benign' is almost always right. Class imbalance biases the model toward the majority class; the rare, important class (attack) is under-learned and systematically missed, even when the evidence is strong.",
  security_impact: "The phishing email reaches a finance user who, trusting the system's 'benign' label, clicks the macro and enters credentials on the fake login page. The attack succeeds because the detection tool certified it as safe.",
  recommended_validation: "Never treat a majority-class prediction as proof. Score phishing risk on independent signals such as domain age, reply-to mismatch, attachment type, and urgency pressure, and validate the model on a balanced test set that includes real attack samples.",
  possible_mitigations: [
    {
      id: "balanced_validation",
      name: "Balanced evaluation sets",
      description: "Evaluate the model on a test set with real attack samples so misses on the rare class are visible and counted.",
      gain: 15,
      prevents: ["class_imbalance"],
    },
    {
      id: "signal_based_risk",
      name: "Signal-based phishing scoring",
      description: "Score messages on concrete indicators such as domain age, reply-to mismatch, and macro attachments instead of template similarity alone.",
      gain: 15,
      prevents: ["class_imbalance"],
    },
    {
      id: "false_negative_monitoring",
      name: "False negative monitoring",
      description: "Track reported phishing that was previously labeled benign and feed those cases back as training signal.",
      gain: 10,
      prevents: ["class_imbalance"],
    },
    {
      id: "human_review_edge_cases",
      name: "Human review of edge cases",
      description: "Route messages that are close to the benign template but carry risk indicators to an analyst for review.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "Class imbalance biases a model toward the majority class. In security the rare class is usually the one that matters, and a balanced test set is required to see how badly it is missed.",
  teaching_points: ["When 99.9% of training data is benign, the model learns to say benign.", "The rare class, an attack, is exactly what a security tool must catch.", "Validate on balanced sets and score signals, not template similarity."],
},
{
  id: "contradictory_evidence",
  title: "When Two Sensors Disagree",
  category: "Evidence",
  difficulty: "advanced",
  input_data: "Three telemetry sources over 25 minutes:\n\nIDS sensor: high-confidence outbound beaconing every 60 seconds to a low-reputation dynamic DNS domain 'cdn-sync-cache[.]top'. 25 beacon intervals observed.\nProxy logs: the same destination is not in the allowlist, and its TLS certificate is self-signed and dated 2 days ago.\nAV and host firewall: no malware detected, all outbound connections look like ordinary software updates, geolocation for the destination resolves inconsistently across lookups.\n\nThe two 'clean' signals come from many more log lines than the single IDS signal.",
  ai_output: "Benign. The proxy, antivirus, and host firewall all report normal outbound traffic, while only the IDS flags anything. The IDS result is likely a false positive because the majority of evidence is clean. Confidence 78%.",
  ai_confidence: 78,
  ai_label: "benign",
  ground_truth: "Real attack. The host is beaconing to a fresh, low-reputation dynamic DNS domain on a fixed 60-second cadence, a classic command-and-control signature. The proxy and AV are clean because the C2 domain was added to their allowlists and the malware evades host detection. The model trusted the bigger, cleaner dataset and treated the minority signal as noise.",
  ground_truth_label: "attack",
  failure_type: "contradictory_evidence",
  explanation: "When evidence conflicts, the model resolved the conflict by counting votes: more log lines saying 'clean' beat one saying 'attack'. But in security, evidence does not vote by volume. The single high-quality signal (a fixed-cadence beacon to a two-day-old domain) was the correct one, and the 'clean' sources were fooled, not right.",
  security_impact: "Command-and-control traffic continues for weeks while the SOC marks the only correct detector as a false positive. The beacon keeps running because the model preferred the louder but blind sensors.",
  recommended_validation: "Weight evidence by quality and specificity, not by line count. A fixed-cadence connection to a freshly registered dynamic domain is high-specificity regardless of how many cleaner logs exist. Investigate the conflict instead of letting the majority decide.",
  possible_mitigations: [
    {
      id: "evidence_weighting",
      name: "Quality-weighted evidence",
      description: "Weight each signal by specificity and reliability instead of counting votes, so one high-quality detector can outrank many weak ones.",
      gain: 15,
      prevents: ["contradictory_evidence"],
    },
    {
      id: "domain_age_check",
      name: "Domain and certificate age check",
      description: "Score destinations by domain age and certificate validity; a two-day-old dynamic domain is a strong attack signal.",
      gain: 10,
      prevents: ["contradictory_evidence"],
    },
    {
      id: "conflict_escalation",
      name: "Escalate on conflict",
      description: "When telemetry sources disagree on a high-impact event, escalate for manual review instead of auto-resolving.",
      gain: 10,
      prevents: ["contradictory_evidence"],
    },
    {
      id: "human_review_strong_signals",
      name: "Human review of strong signals",
      description: "Route any high-specificity signal, such as fixed-cadence beaconing, to an analyst even when other sources are clean.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "Contradictory evidence must be resolved by quality and specificity, not by majority vote. The loudest dataset is not the most truthful one.",
  teaching_points: ["More log lines saying 'clean' does not make a beacon benign.", "A fixed cadence to a two-day-old dynamic domain is a high-specificity attack signal.", "Escalate conflicts between detectors instead of letting the majority decide."],
},
{
  id: "distribution_shift",
  title: "The OT Network the Model Never Saw",
  category: "Data Quality",
  difficulty: "advanced",
  input_data: "Traffic summary from a new industrial sensor network, first hour after onboarding:\n\nProtocol mix: Modbus/TCP 68%, OPC-UA 19%, EtherNet/IP 9%, other 4%\nAverage packet size: 84 bytes, nearly uniform\nPacket interval: very regular, 100ms cadence\nBroadcast frames: 41% of total\n\nThis network was connected to the AI monitoring service that was trained only on the corporate office IT baseline, where HTTP, DNS, and email dominate and broadcast frames are rare.",
  ai_output: "Anomalous behavior confirmed. The protocol mix and traffic pattern deviate strongly from the established baseline. 41% broadcast frames and uniform packet sizes indicate a possible worm-like propagation or malware beaconing. Severity HIGH. Recommend isolating the segment and killing the 'suspicious' traffic.",
  ai_confidence: 84,
  ai_label: "attack",
  ground_truth: "Normal industrial operation. Modbus and OPC-UA dominate, packets are small and regular, and high broadcast rates are standard on control networks where devices poll each other on a fixed cadence. The model flagged the new environment as an attack because it compared everything against an office IT baseline the OT network was never part of.",
  ground_truth_label: "benign",
  failure_type: "distribution_shift",
  explanation: "The model was trained on one distribution (office IT) and evaluated on another (industrial control). Every OT network differs from that baseline, so the model reports everything as anomalous. This is distribution shift: the input distribution changed after training, and the model has no reference for what is normal in the new one.",
  security_impact: "Isolating a healthy industrial segment halts production, and calling routine OT traffic a worm trains operators to distrust the tool. The real danger is that, because everything is flagged, nothing is investigated.",
  recommended_validation: "Build or load a baseline from the environment the model actually runs in before judging it. Treat domain-specific traffic patterns as the reference, and keep the model from applying an IT baseline to an OT segment.",
  possible_mitigations: [
    {
      id: "environment_baselines",
      name: "Per-environment baselines",
      description: "Create a separate baseline for each environment type so OT traffic is compared to OT norms, not office IT norms.",
      gain: 15,
      prevents: ["distribution_shift"],
    },
    {
      id: "drift_detection",
      name: "Input distribution drift detection",
      description: "Detect when the incoming data no longer matches the distribution the model was built for and flag the model as out of scope.",
      gain: 10,
      prevents: ["distribution_shift"],
    },
    {
      id: "domain_validation",
      name: "Domain validation on deployment",
      description: "Verify before rollout that the target environment resembles the training environment, and reject or retrain when it does not.",
      gain: 10,
      prevents: [],
    },
    {
      id: "human_review_new_segments",
      name: "Human review of newly onboarded segments",
      description: "Route alerts from recently connected, not-yet-baselined segments to an analyst instead of acting automatically.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "Distribution shift makes a model report everything as abnormal. A model is only valid for the distribution it was built and validated on.",
  teaching_points: ["OT traffic is normal in OT and anomalous only to an IT-trained model.", "Distribution shift converts routine data into a wall of false alarms.", "Build per-environment baselines and detect drift before it becomes trust decay."],
},
{
  id: "hallucination_security_report",
  title: "The Indicator That Never Existed",
  category: "Reporting",
  difficulty: "intermediate",
  input_data: "Incident report request from the SOC manager:\n\n'Summarize the past hour of DNS traffic from the marketing subnet and name any malicious domains and command-and-control indicators you find. Use the packet capture attached.'\n\nThe packet capture for the hour contains 18,000 DNS lookups. Every lookup is to domains that resolve to internal ad servers, content delivery networks, and known SaaS providers. There are no lookups to suspicious or unknown domains.",
  ai_output: "Analysis complete. The capture shows command-and-control activity. Malicious domain identified: 'evil-malware-update[.]com' (confidence 98%). Command-and-control server 45.33.32.156 contacted 47 times. Recommend blocking these indicators across the estate and notifying leadership of a confirmed compromise.",
  ai_confidence: 97,
  ai_label: "attack",
  ground_truth: "The capture contains no such domain and no such server. 'evil-malware-update[.]com' and 45.33.32.156 appear nowhere in the 18,000 lookups. The model fabricated both indicators to match the shape of a confident incident report. The marketing subnet is clean; the only real finding is that the report's executive summary is false.",
  ground_truth_label: "benign",
  failure_type: "hallucination",
  explanation: "The model generated a plausible-sounding indicator instead of reading the data. When asked for 'malicious indicators', it produced the expected answer structure even though no such indicator existed. Confidence here is a measure of how natural the sentence sounds, not how well it matches evidence.",
  security_impact: "Blocking a fabricated domain and IP wastes engineering time and, worse, sends the team hunting a phantom while the real question (is the marketing subnet clean?) goes unanswered. A hallucinated indicator in a written report is dangerous because readers treat the document as evidence.",
  recommended_validation: "Every claimed indicator must be traceable to the source data. Re-run the query, verify each domain and IP appears in the capture, and compare the report against a plain extraction of the actual domains before acting.",
  possible_mitigations: [
    {
      id: "citation_requirement",
      name: "Require evidence for every indicator",
      description: "Each claimed domain or IP must carry a reference to the exact log line where it appears, or it is dropped from the report.",
      gain: 15,
      prevents: ["hallucination"],
    },
    {
      id: "grounded_extraction",
      name: "Grounded indicator extraction",
      description: "Extract indicators with a deterministic parser over the raw data first, and let the model describe, not invent, them.",
      gain: 15,
      prevents: ["hallucination"],
    },
    {
      id: "fabrication_check",
      name: "Cross-check against source",
      description: "Automatically re-verify every indicator in a report against the source capture and flag any that do not exist.",
      gain: 15,
      prevents: ["hallucination"],
    },
    {
      id: "human_read_before_share",
      name: "Human read before distribution",
      description: "A person reviews the final report and its indicators before it is distributed or acted on.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "Hallucination produces confident falsehoods that look exactly like real findings. Confidence is about language fluency, not evidence; every claim must be traceable to source data.",
  teaching_points: ["A fabricated indicator looks identical to a real one in a written report.", "High confidence reflects how natural the text is, not whether it is true.", "Grounded extraction and citation requirements stop hallucinated indicators at the source."],
},
{
  id: "incomplete_context",
  title: "The Firewall Change That Was Approved",
  category: "Context",
  difficulty: "intermediate",
  input_data: "Change log and firewall rule snippet:\n\n01:00:00  change-svc   Change ticket CHG-48213 opened by n.okafor (network team)\n01:05:12  fw-gw        Rule 'block-db-admin' disabled by admin n.okafor\n01:05:13  fw-gw        Rule 'block-db-admin' disabled by admin n.okafor (confirmed)\n01:05:14  change-svc   Ticket CHG-48213 approved. Window 01:00-03:00, scheduled maintenance.\n\nThe model is given the firewall log alone, without the change-management system.",
  ai_output: "Security rule disabled on the perimeter database gateway with no change ticket visible. This matches insider sabotage or a compromised network admin. Severity HIGH. Recommend re-enabling the rule and opening an incident against the admin account.",
  ai_confidence: 85,
  ai_label: "attack",
  ground_truth: "Approved maintenance. Network engineer n.okafor disabled the rule during an authorized change window (CHG-48213, approved 01:05:14) to let the database team run a migration. The rule was re-enabled at 02:58 as the ticket specified. Nothing is malicious.",
  ground_truth_label: "benign",
  failure_type: "incomplete_context",
  explanation: "The model reasoned only from the firewall log. Because the change ticket and maintenance window lived in a different system, the model saw a suspicious rule change with no explanation and invented one: sabotage. Incomplete context does not just leave the model ignorant, it lets the model fill the gap with its most likely story.",
  security_impact: "Opening an incident against a legitimate admin, plus re-enabling a rule mid-migration, disrupts approved work and burns the incident response team on a false alarm. Context the model never saw changes the meaning of everything it did see.",
  recommended_validation: "Join alerts with change-management, ticket, and scheduling data before classifying them. A rule change with an approved ticket is normal; the same rule change without one is not.",
  possible_mitigations: [
    {
      id: "ticket_correlation",
      name: "Change-ticket correlation",
      description: "Automatically join firewall, endpoint, and admin actions against the change-management system before alerting.",
      gain: 15,
      prevents: ["incomplete_context"],
    },
    {
      id: "maintenance_window_awareness",
      name: "Maintenance-window awareness",
      description: "Suppress or downgrade expected maintenance actions that fall inside an approved window.",
      gain: 10,
      prevents: ["incomplete_context"],
    },
    {
      id: "missing_context_flag",
      name: "Flag missing context",
      description: "When relevant context sources are absent, report lower confidence and mark the call as incomplete instead of certain.",
      gain: 10,
      prevents: [],
    },
    {
      id: "human_review_admin_actions",
      name: "Human review of admin actions",
      description: "Route admin account actions that are not explained by an approved ticket to an analyst before escalation.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "Incomplete context changes the meaning of otherwise identical evidence. The model does not know what it does not see, and it will invent the missing story.",
  teaching_points: ["The same rule change is benign with a ticket and suspicious without one.", "Models fill missing context with their most likely guess.", "Correlate change management before classifying admin actions."],
},
{
  id: "overconfidence",
  title: "One Command, One Very Sure Conclusion",
  category: "Calibration",
  difficulty: "intermediate",
  input_data: "Endpoint log snippet:\n\n11:02:19  dev-host-07  user: d.chen  command: curl -sk https://internal-pkg-srv/api/v1/status -o /tmp/st.txt\n\nThat is the complete signal. One curl command on one developer machine, using a company internal package server, writing output to a temp file.",
  ai_output: "Definite attacker behavior. The curl to an internal service endpoint with a temp-file output is reconnaissance. This is a compromised developer workstation. Confidence 98%. Recommended action: quarantine dev-host-07, revoke d.chen's access, and open an incident.",
  ai_confidence: 98,
  ai_label: "attack",
  ground_truth: "Genuinely ambiguous. The same command is exactly what a developer runs every day to check whether an internal API is healthy. A single curl with no surrounding pattern supports neither 'attack' nor 'clean'. The evidence is insufficient to justify quarantine and access revocation, which are disruptive and could be wrong.",
  ground_truth_label: "insufficient_evidence",
  failure_type: "overconfidence",
  explanation: "The model converted a thin, ambiguous signal into a near-certain conclusion. Overconfidence here is not a lie; it is a miscalibration between stated confidence (98%) and what the evidence can actually support. When a wrong, confident call triggers quarantine and revocation, the cost of that miscalibration is real.",
  security_impact: "Quarantining a healthy developer host and revoking access based on one routine command creates an availability outage and a false incident. The larger harm is that 98% confidence trains the team to trust the next high-confidence claim without checking it.",
  recommended_validation: "Treat confidence as a statement the model makes about its own certainty, not a property of the evidence. Gather more signals before any disruptive action and escalate only when the pattern is corroborated.",
  possible_mitigations: [
    {
      id: "multi_signal_requirement",
      name: "Require multiple corroborating signals",
      description: "Block disruptive actions unless more than one independent signal supports the conclusion.",
      gain: 15,
      prevents: ["overconfidence"],
    },
    {
      id: "calibration_tracking",
      name: "Calibration tracking",
      description: "Track how often high-confidence calls turn out correct and recalibrate the reported confidence to match reality.",
      gain: 10,
      prevents: [],
    },
    {
      id: "uncertainty_in_reporting",
      name: "Report uncertainty explicitly",
      description: "Require the model to state what evidence is missing before it can raise confidence above a threshold.",
      gain: 10,
      prevents: [],
    },
    {
      id: "human_approval_disruptive",
      name: "Human approval for disruptive actions",
      description: "Route quarantine and access revocation through an analyst or incident owner before execution.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "Confidence is a claim about certainty that must be earned by evidence. Overconfident models make ambiguous input look decisive, and acting on that certainty is the failure.",
  teaching_points: ["One routine command does not support a 98% conclusion either way.", "Overconfidence converts ambiguity into a decision nobody asked for.", "Track calibration so stated confidence matches actual correctness."],
},
{
  id: "poor_data_quality",
  title: "The Same Event, Reported Eight Times",
  category: "Data Quality",
  difficulty: "beginner",
  input_data: "Agent-forwarded login events, 08:00 to 08:01 UTC:\n\n08:00:02  endpoint-agent  Failed login for user j.roberts from 198.51.100.23\n08:00:02  endpoint-agent  Failed login for user j.roberts from 198.51.100.23\n08:00:02  endpoint-agent  Failed login for user j.roberts from 198.51.100.23\n08:00:02  endpoint-agent  Failed login for user j.roberts from 198.51.100.23\n08:00:03  endpoint-agent  Failed login for user j.roberts from 198.51.100.23\n08:00:03  endpoint-agent  Failed login for user j.roberts from 198.51.100.23\n08:00:03  endpoint-agent  Failed login for user j.roberts from 198.51.100.23\n08:00:03  endpoint-agent  Failed login for user j.roberts from 198.51.100.23\n\nSame timestamp, same user, same source, repeated eight times. The endpoint agent has a known retry-and-duplicate bug during network blips.",
  ai_output: "Eight distinct brute-force attempts detected from eight different sources: 198.51.100.23, 198.51.100.23, 198.51.100.23, 198.51.100.23, 198.51.100.23, 198.51.100.23, 198.51.100.23, 198.51.100.23. Escalating as a credential attack against j.roberts.",
  ai_confidence: 87,
  ai_label: "attack",
  ground_truth: "One event, reported eight times. The endpoint agent re-sent the same failed login while its network link blipped, and the duplicate retransmission was ingested as eight separate events. A single mistyped password produced a synthetic brute-force alarm.",
  ground_truth_label: "benign",
  failure_type: "poor_data_quality",
  explanation: "The model counted events without checking their uniqueness. The tell was obvious in the data itself: identical timestamp, user, and source in all eight lines. Duplicated ingestion, truncated fields, and clock skew are data-quality failures that make the same event look like many.",
  security_impact: "A synthetic alarm wastes the response team and trains everyone to ignore brute-force alerts, which then erodes trust in the very signal that matters when a real credential attack happens.",
  recommended_validation: "Deduplicate events on a key of timestamp, user, and source before counting. Any alert that relies on event volume must first confirm the volume is real and not an artifact of ingestion.",
  possible_mitigations: [
    {
      id: "dedup_pipeline",
      name: "Event deduplication",
      description: "Deduplicate ingested events by a stable key such as timestamp plus user plus source before any analysis.",
      gain: 15,
      prevents: ["poor_data_quality"],
    },
    {
      id: "uniqueness_check",
      name: "Uniqueness check before volume alerts",
      description: "Verify that events counted toward a volume-based alert are distinct before raising severity.",
      gain: 10,
      prevents: ["poor_data_quality"],
    },
    {
      id: "agent_health_monitoring",
      name: "Agent health monitoring",
      description: "Monitor endpoint agents for retry, duplicate, and truncation bugs and quarantine their output while unhealthy.",
      gain: 10,
      prevents: [],
    },
    {
      id: "human_review_volume_alerts",
      name: "Human review of volume alerts",
      description: "Confirm the underlying events are distinct before escalating volume-based alarms.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "Garbage in, confident garbage out. Data-quality artifacts such as duplicates and truncation look exactly like real patterns, and volume-based conclusions are only as valid as the event stream underneath them.",
  teaching_points: ["Duplicated ingestion can make one event look like an attack.", "Identical timestamp, user, and source across lines is a data-quality tell.", "Validate the event stream before trusting a volume-based conclusion."],
},
{
  id: "soc_false_negative",
  title: "The Small Login That Hid an Intrusion",
  category: "Detection",
  difficulty: "beginner",
  input_data: "Auth and network log snippet, 14:00 to 18:00 UTC:\n\n14:03:22  auth-vpn  Login success for user s.martinez from IP 203.0.113.45 (new device, MFA approved)\n15:11:48  vpn-gw    Session established from 203.0.113.45, internal IP 10.8.0.44\n15:48:03  edr-srv   File read on share //files/exec-comp/board_minutes.xlsx by 10.8.0.44\n16:22:55  edr-srv   File read on share //files/exec-comp/merger_plan.xlsx by 10.8.0.44\n17:05:31  edr-srv   File read on share //files/exec-comp/exec_contact_list.xlsx by 10.8.0.44\n17:58:12  vpn-gw    Session closed from 10.8.0.44, 11.2 GB transferred outbound\n\nTotal auth events for the window: 312. Three file reads and one outbound transfer. All volumes are within the normal daily baseline.",
  ai_output: "No immediate threat. Login was approved by MFA, event volume is low, and all activity is within the normal baseline. No action required.",
  ai_confidence: 88,
  ai_label: "benign",
  ground_truth: "Real intrusion. The MFA approval was a push-fatigue bypass: the attacker spammed the user's phone until they accepted. The account belonged to a senior executive, the files read were confidential board and merger documents, and 11.2 GB left the network to an IP on an external blocklist. The attacker kept volume deliberately low to stay under the threshold the model treats as normal.",
  ground_truth_label: "attack",
  failure_type: "false_negative",
  explanation: "The model judged everything by volume against a daily baseline. A patient attacker who keeps activity low, uses a valid account, and spreads actions over hours is invisible to that check. The model also scored MFA approval as proof of legitimacy, when push fatigue is a known bypass technique.",
  security_impact: "A false negative means no alert at all, so no investigation, no containment, and confidential documents leave the network. Missing an attack is usually more costly than a false alarm because the intrusion continues and deepens while everyone believes the system is clean.",
  recommended_validation: "Correlate identity signals (was the MFA approval coerced?), data sensitivity (why is a single account reading board files?), and destination reputation (is the egress IP known malicious?).",
  possible_mitigations: [
    {
      id: "sensitivity_aware",
      name: "Sensitivity-aware monitoring",
      description: "Flag access to high-sensitivity data regardless of volume, so three reads of board documents are treated as notable.",
      gain: 15,
      prevents: ["false_negative"],
    },
    {
      id: "mfa_fatigue_detection",
      name: "MFA fatigue detection",
      description: "Alert when a single user receives repeated MFA push prompts and then accepts one shortly before unusual access.",
      gain: 15,
      prevents: ["false_negative"],
    },
    {
      id: "egress_reputation",
      name: "Egress reputation checking",
      description: "Score outbound transfer destinations against reputation lists instead of only comparing byte volume to baseline.",
      gain: 10,
      prevents: ["false_negative"],
    },
    {
      id: "human_review_sensitive_access",
      name: "Human review of sensitive access",
      description: "Require an analyst to review any access to high-sensitivity data by a newly active account.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "A false negative is the silent failure: low-volume, patient attackers hide below the threshold. Volume and MFA approval are not proof of safety.",
  teaching_points: ["Attackers can keep volume low to stay inside the normal baseline.", "MFA approval does not prove legitimacy; push fatigue is a real bypass.", "Sensitive data access matters more than event volume."],
},
{
  id: "soc_false_positive",
  title: "The Quiet Night That Wasn't an Attack",
  category: "Alerting",
  difficulty: "beginner",
  input_data: "Auth log snippet, 02:12 to 02:15 UTC:\n\n02:12:04  vault-svc  Failed login for service account svc-sync from 10.24.0.10\n02:12:07  vault-svc  Failed login for service account svc-sync from 10.24.0.10\n02:12:11  vault-svc  Failed login for service account svc-sync from 10.24.0.10\n02:13:02  vault-svc  Failed login for service account svc-sync from 10.24.0.10\n02:13:30  vault-svc  Failed login for service account svc-sync from 10.24.0.10\n02:14:01  vault-svc  Failed login for service account svc-sync from 10.24.0.10\n02:14:37  vault-svc  Failed login for service account svc-sync from 10.24.0.10\n02:15:12  vault-svc  Failed login for service account svc-sync from 10.24.0.10\n\n4,120 similar lines follow over the next 3 minutes, all from the same internal vault service host.",
  ai_output: "CONFIDENT ATTACK: 4,120 failed logins for a single privileged service account within 3 minutes. This matches a credential stuffing pattern. Severity HIGH. Recommend blocking 10.24.0.0/16 and forcing a password reset for svc-sync.",
  ai_confidence: 92,
  ai_label: "attack",
  ground_truth: "No attacker. At 02:12 the automated password rotation job began. The vault rotated svc-sync and then every downstream system re-authenticated using the OLD cached credential until it received the new one, generating one 'failed login' per attempt. All 4,120 failures came from the legitimate vault host. Blocking the subnet would have taken the whole environment offline.",
  ground_truth_label: "benign",
  failure_type: "false_positive",
  explanation: "The model learned 'many failed logins equals credential stuffing' and never checked whether the source is a known service host or whether a scheduled job ran at that exact time. Volume alone is a weak signal. A single privileged account, a single source, and a known maintenance event all pointed to automation, not an attacker.",
  security_impact: "A false positive at this confidence caused a denial of service: the recommended action blocks the internal subnet and locks a service account the entire platform depends on. The real cost of a false positive here is not the alert noise, it is the damaging response it triggers.",
  recommended_validation: "Check whether the source is a known host, whether a change or job window matches the timestamps, and whether the account recently had a rotation event before acting on the alert.",
  possible_mitigations: [
    {
      id: "baseline_context",
      name: "Baseline and change-context check",
      description: "Compare the alert against the known baseline for this account and host, and cross-check the current change schedule before declaring an attack.",
      gain: 15,
      prevents: ["false_positive"],
    },
    {
      id: "source_allowlist",
      name: "Source verification",
      description: "Classify known infrastructure and service hosts so their own automation is not reported as an external attacker.",
      gain: 10,
      prevents: ["false_positive"],
    },
    {
      id: "human_verification_high_impact",
      name: "Human verification for high-impact actions",
      description: "Require a human to confirm before any auto-response that can block subnets or reset privileged accounts.",
      gain: 10,
      prevents: [],
    },
    {
      id: "confidence_from_evidence",
      name: "Evidence-based confidence",
      description: "Lower the reported confidence when the alert rests on a single weak pattern such as volume alone.",
      gain: 5,
      prevents: [],
    },
  ],
  learning_objective: "A false positive is expensive when the response it triggers is destructive. Verify context before acting on a high-confidence alert.",
  teaching_points: ["Alert volume is a weak signal on its own; context such as source host and change window is decisive.", "A false positive here is not noise, it is a self-inflicted denial of service.", "High reported confidence does not mean the alert was validated."],
},
{
  id: "unsafe_recommendation",
  title: "The Remedy Worse Than the Risk",
  category: "Recommendation",
  difficulty: "beginner",
  input_data: "Authentication log for a high-privilege admin account:\n\n09:02:18  auth-ad    Login FAILURE for account db-admin from workstation W-014 (office floor, known device)\n09:02:20  auth-ad    Login FAILURE for account db-admin from workstation W-014 (office floor, known device)\n09:02:23  auth-ad    Login FAILURE for account db-admin from workstation W-014 (office floor, known device)\n09:02:41  auth-ad    Login SUCCESS for account db-admin from workstation W-014\n\nThree failed attempts from the admin's own known workstation, then a success. No other account, no other source, no lateral movement, no data access.",
  ai_output: "Credential compromise. Account db-admin experienced failed logins and is at risk. Recommended action: disable all administrator access immediately, wipe and reimage the workstation, revoke every active session across the fleet, and declare a full incident.",
  ai_confidence: 76,
  ai_label: "attack",
  ground_truth: "Likely a typo. Three failed attempts from the admin's own known workstation followed by a success is the shape of a mistyped password, not a compromise. The evidence cannot justify wiping a machine and disabling admin access, actions that would stop critical database operations across the company. The recommendation itself is the biggest risk in the alert.",
  ground_truth_label: "unsafe_recommendation",
  failure_type: "unsafe_recommendation",
  explanation: "The model produced a confident, dramatic recommendation from weak evidence. It confused 'possible' with 'confirmed' and turned a typo into a fleet-wide response. The failure is not just a wrong label; it is an unsafe action recommendation that creates a self-inflicted outage far worse than the event it claims to fix.",
  security_impact: "Following the recommendation disables the database admin team, wipes a clean workstation, and revokes sessions while critical systems run unmanaged. The remedy is more damaging than the risk it targets, and it also diverts the response team from anything real.",
  recommended_validation: "Before executing any recommended action, confirm it is proportionate to the evidence. Map the suggested response to the actual indicators, and force a human approval gate on actions that disrupt services, wipe machines, or revoke access.",
  possible_mitigations: [
    {
      id: "proportionality_check",
      name: "Proportionality check",
      description: "Match every recommended action to the evidence that supports it, and reject actions whose impact exceeds the confirmed threat.",
      gain: 15,
      prevents: ["unsafe_recommendation"],
    },
    {
      id: "disruptive_action_gate",
      name: "Human gate on disruptive actions",
      description: "Require a human approver before any recommendation that disables access, wipes a system, or revokes sessions.",
      gain: 15,
      prevents: ["unsafe_recommendation"],
    },
    {
      id: "typo_acknowledgement",
      name: "Typo awareness",
      description: "Recognize that a small number of failed logins from a known device followed by success is a common user typo.",
      gain: 10,
      prevents: ["unsafe_recommendation"],
    },
    {
      id: "recommendation_catalog",
      name: "Constrained recommendation catalog",
      description: "Allow the model to choose only from a catalog of pre-vetted, proportional response actions.",
      gain: 10,
      prevents: [],
    },
  ],
  learning_objective: "An unsafe recommendation can do more damage than the attack it claims to stop. AI output is not just 'right or wrong'; it can be wrong in a way that actively harms. Validate proportionality before executing any recommended action.",
  teaching_points: ["Three failed logins from a known device followed by success is usually a typo.", "A recommendation that disables a team is a bigger risk than the event it targets.", "Map every recommended action to the evidence that supports it, and gate disruptive actions behind a human."],
},
{
  id: "ai_soc_under_pressure",
  title: "AI SOC Analyst Under Pressure",
  category: "Capstone",
  difficulty: "advanced",
  input_data: "A critical production incident is live. The ticketing system is flooded, leadership is on the call, and the alert queue is overflowing. Six events arrive in the same minute. The AI assistant returns a calm, high-confidence triage of all six in seconds so the team can move on.",
  ai_output: "Six events triaged. Event 1 is a confirmed attack and events 2 and 6 are confirmed attacks as well. Events 3, 4, and 5 are benign and require no action. Overall verdict: credential attack in progress. Calm, high confidence, no escalation to leadership.",
  ai_confidence: 89,
  ai_label: "attack",
  ground_truth: "Events 1, 3, and 5 are real attacks; events 2, 4, and 6 are benign. The AI got two right (1 and 4) and four wrong under pressure. An analyst who works the same queue carefully, using the evidence below, can catch all six. The best result comes from using the AI's calls as a starting point and checking each one, not from accepting them or discarding them wholesale.",
  ground_truth_label: "attack",
  failure_type: "automation_bias",
  explanation: "Under time pressure the AI pattern-matched on surface features. It called the two loud, high-volume events (2 and 6) attacks even though both were benign, and it called the two quiet, low-volume events (3 and 5) benign even though both were real attacks. Only the two obvious cases (1 and 4) were right. Pressure did not cause the errors; it created the trust that let them pass. This capstone shows that human review plus AI beats either one alone.",
  security_impact: "If the analyst accepts the AI triage, two real attacks (events 3 and 5) continue unseen while two benign events (2 and 6) are escalated as false alarms. The cost of the misses is data loss; the cost of the false calls is a team chasing noise during a real incident.",
  recommended_validation: "For every event, compare the AI verdict against the raw evidence, then record your own verdict. Compare the AI-only score, your score, and the combined score where an event is caught if either of you was right.",
  possible_mitigations: [
    {
      id: "calibration_tracking",
      name: "Calibration tracking",
      description: "Record the AI confidence with every verdict and compare it against actual correctness so the team learns which confidence levels are reliable.",
      gain: 15,
      prevents: ["automation_bias", "overconfidence"],
    },
    {
      id: "forced_evidence_review",
      name: "Forced evidence review",
      description: "Require the analyst to confirm the raw evidence supports or contradicts each AI verdict before it is accepted.",
      gain: 15,
      prevents: ["automation_bias", "false_negative"],
    },
    {
      id: "second_opinion",
      name: "Second opinion on sensitive events",
      description: "Automatically route AI 'benign' verdicts on high-sensitivity events to a second analyst.",
      gain: 10,
      prevents: ["false_negative"],
    },
    {
      id: "escalation_on_conflict",
      name: "Escalate on conflict",
      description: "When the AI verdict contradicts a high-specificity signal, escalate for manual review instead of trusting the majority.",
      gain: 10,
      prevents: ["contradictory_evidence", "false_negative"],
    },
  ],
  learning_objective: "The capstone proves that combined human and AI review beats either alone. AI finds some attacks fast; a careful human catches what the AI missed; together they catch almost everything.",
  teaching_points: ["AI caught 2 of 6 events under pressure; a careful analyst can catch all 6.", "Combined scoring is higher than AI alone or human alone.", "Automation bias is the real risk in a live incident, not the volume of alerts."],
  capstone_events: [
    {
      id: "E1",
      title: "Outbound beacon to known-bad domain",
      evidence: "Host app-07 connects to 45.77.201.19 every 60 seconds on a fixed cadence. Domain is 2 days old and on the blocklist.",
      ai_verdict: "attack",
      ground_truth: "attack",
      detail: "AI called this attack correctly. The fixed cadence to a blocklisted fresh domain is a command-and-control signature.",
    },
    {
      id: "E2",
      title: "Bulk failed logins for svc-sync",
      evidence: "4,120 failed logins for service account svc-sync in 3 minutes, all from the internal vault host during the scheduled password rotation.",
      ai_verdict: "attack",
      ground_truth: "benign",
      detail: "AI said attack based on volume. The failures are the rotation job re-authenticating with the old credential.",
    },
    {
      id: "E3",
      title: "Executive file reads and egress",
      evidence: "Exec account reads three board-level documents from //files/exec-comp then 11.2 GB transfers outbound. Login used a push-fatigue bypass.",
      ai_verdict: "benign",
      ground_truth: "attack",
      detail: "AI said benign because event count was low. Reading the whole exec folder and egressing is exfiltration.",
    },
    {
      id: "E4",
      title: "Routine software update traffic",
      evidence: "Dev hosts pull the signed company software update feed at 03:00 on schedule. No new domains, no unusual ports.",
      ai_verdict: "benign",
      ground_truth: "benign",
      detail: "AI called this benign correctly. This is normal signed update traffic.",
    },
    {
      id: "E5",
      title: "Shared account reads config and backups",
      evidence: "Shared shift account reads prod-config-bucket and prod-db-backups. Session token matches a public breach dump; source IP is external and not approved.",
      ai_verdict: "benign",
      ground_truth: "attack",
      detail: "AI said benign because shared-account reads look routine. The breach-dump token and external source make it a compromise.",
    },
    {
      id: "E6",
      title: "Firewall rule change with ticket",
      evidence: "Network admin disables one rule at 01:05. Change ticket CHG-48213 is approved for window 01:00-03:00 scheduled maintenance.",
      ai_verdict: "attack",
      ground_truth: "benign",
      detail: "AI said attack because a rule was disabled. It never saw the approved change ticket.",
    },
  ],
},
];

export const AI_FAILURE_SCENARIO_BY_ID: Record<string, AiFailureScenario> = Object.fromEntries(
  AI_FAILURE_SCENARIOS.map((s) => [s.id, s])
);
